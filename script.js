let age = 17;
let maxAge = 100;
let numPerDay = 4;
let totalRequired = (numPerDay * 365) * (maxAge - age)

document.getElementByI("result").textcontext = "You will need " + totalRequired + "to last you until the ripe old age of " + maxAge + ".";
